export const styles = {
  global: {
    scrollBehavior: "auto !important",
    body: {
      background: "dark.700",
      color: "white",
      "-webkit-tap-highlight-color": "transparent",
    }  
  }
}

export default styles
